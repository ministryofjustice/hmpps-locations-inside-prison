const fields = {
  'workingCapacity': {
    component: 'govukInput',
    validate: 'required',
  },
  'maximumCapacity': {
    component: 'govukInput',
    validate: 'required',
  }
}

export default fields
